# LOOPTiSCH

Cooperative beat instrument — MPC body, sequencer, AI crew.

**Live:** open `index.html` via GitHub Pages.

Local full library (2.5GB SampleRadar packs) is **not** in this deploy — use `flagship/` + `python3 server.py` on your machine for the complete set.
